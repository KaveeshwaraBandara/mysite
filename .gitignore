# Virtual environment — regenerable, machine-specific
venv/

# Python cache files
__pycache__/
*.pyc

# Environment variables / secrets (we'll use this later)
.env

# Editor and OS noise
.vscode/
.DS_Store
app.db
